# Clarification Log — p1-deck-geometry

*Design clarification loop per charter §3.2a. Grok asks numbered questions; Kimi answers by number; answers are binding rulings. Design changes from this log require a design-doc version bump BEFORE spec finalization.*

`KIMI RULINGS v1 | design pack v1 | design doc v1.10 → v1.11 | visual direction v1.1 → v1.2 | 2026-07-29`

| Q# | Grok's question | Kimi's ruling | Doc change? |
|----|-----------------|---------------|-------------|
| 1 | **Unmarked inter-room faces (pack clarification note / §6.1).** Faces not marked A/B/C on the mockup (e.g. room-to-spine walls between doorways) are described as solid interior walls — “Class B for behavior” while keeping the mockup’s visual distinction. Confirm: (a) these segments are authored `wallClass: B` for all validation and future damage rules; (b) they are *not* “bulkhead law” section bounds (gated-section firewall); (c) whether the design doc needs an explicit fourth presentation category or a `role: interior` qualifier under Class B. | **(a) Yes** — authored `wallClass: B`. **(b) Correct** — only the three marked bulkhead bands (aft rear, mid\|aft, mid\|fore) are section bounds / the gating firewall. **(c)** Class B gets two *presentations*, not a new class: **bulkhead** (section-bounding bands — §7 r7 firewall) and **interior** (ordinary solid walls; identical immunity, not section bounds). This is now written into design doc §6.1 (v1.11). | **yes → design doc v1.11** |
| 2 | **Per-room accent tint mapping (G3 / visual direction §3).** Pack sections are Aft / Midships / Fore, but the accent palette has six function tints (Engineering, Command, Ops, Life support, Crew, Hydro) plus Safe for airlocks. Please provide the binding `accentId` (or hex) for every node, especially Midships mixed rooms: Cargo, Armory, Med Bay, Mess, Main Spine, Fore Connector, both Airlocks, Officer Qtrs, Comms/Nav. Spec provisionally maps airlocks→Safe `#69f0ae`, cargo/armory/med/spine/connector→Ops, barracks/officer/mess→Crew, etc. Confirm, replace, or publish a full 18-row table. | **Rejected provisional; binding table below** (canonical = the mockup's own per-room colors, v2.2 — VD §3 inherits them, now extended explicitly in v1.2): Port Engine `#ef5350` · Stbd Engine `#ef5350` · Engineering `#ef5350` · Main Spine `#8b949e` (corridor-neutral) · Port Airlock `#69f0ae` · Cargo Hold `#ffb74d` · Armory `#ff8a65` · Med Bay `#80cbc4` · Life Support `#4dd0e1` · Barracks `#ce93d8` · Stbd Airlock `#69f0ae` · Hydroponics `#81c784` · Mess `#a1887f` · Fore Connector `#8b949e` · Officer Qtrs `#ce93d8` · CIC `#7986cb` · Comms/Nav `#5c7cfa` · Bridge `#7986cb`. | **yes → visual direction v1.2** (extends §3 accent table: armory `#ff8a65`, medical `#80cbc4`, comms `#5c7cfa`, mess `#a1887f`, corridor-neutral `#8b949e` — all inherited from mockup v2.2, now explicit) |
| 3 | **Critical-path set for §7 r8 at P1 (G2).** Rule 8 requires every critical-path room to border ≥1 Class A wall. With no gates/keys yet, which node ids are on the critical path for this deck’s validator? In particular: does Main Spine count (it has no Class A partition in the pack’s 9), do both airlocks count, does Engineering count (Class-B-bounded — may border zero Class A), does Bridge count? Please list the exact node set and whether any path rooms are exempt from the Class A adjacency check until P2 gating exists. | **P1 critical-path set = { Port Airlock, Stbd Airlock, Main Spine, Engineering }** (the P1 traversal: breach → spine → Engineering). The r8 Class-A-adjacency check applies to the **airlocks only** (both pass: each borders ≥1 Class A partition). **Exemptions:** Main Spine (corridor = pacing axis, §7 r4 — flanking is provided by the Class A chains in the room rows beside it, not by breaching the spine itself); Engineering (Class-B-bounded objective heart — the firewall is the point). Fore nodes (connector, CIC, officer qtrs, comms/nav, Bridge) are **off-path at P1** — exempt until P2 gating; note CIC already borders 2 Class A partitions, so it is future-compliant. Re-check this set when `p2-lockdown` lands. | no |
| 4 | **Spawn-point placement and clearance (G5).** For each spawn-safe airlock, where exactly should the authored spawn point sit (geometric center of footprint, offset toward spine door, offset toward hull breach marker)? What horizontal clearance beyond a standard marine capsule is required (spec provisionally uses proxy radius 0.4 m + 0.2 m clearance)? Is any vertical/floor offset required, or is `y = 0` on the floor plane correct for P1 markers? | **Placement:** room center shifted **1.0 m toward the spine-side door**, orientation facing that door (the squad steps in moving toward the ship's interior, hull breach at their backs). **Clearance:** proxy radius 0.4 m + **0.3 m** clearance (confirmed). **Vertical:** `y = 0` on the floor plane is correct for P1 markers. | no |
| 5 | **Interior ceiling height (G3 / M6).** Visual direction mandates full interiors including ceilings but does not state floor-to-ceiling meters. What blockout height should Deck 03 use for all rooms (single constant)? Spec provisionally uses `3.0` m. | **Confirmed: 3.0 m** single constant for all Deck 03 rooms at blockout. (Hero-height variation — e.g., reactor double-height, §12.3 — is a later-phase art decision, not this feature.) | no |
| 6 | **Bridge footprint polygon (G1).** Pack inventory lists a 3-point wedge `(933,352 → 1112,400 → 933,448)`, while `gen_ship_svg.py` authors a 7-point polygon `933,352 1044,374 1104,395 1112,400 1104,405 1044,426 933,448`. Which is canonical for the machine-readable definition and collision? | **The 7-point SVG polygon is canonical** (the pack's 3-point wedge was shorthand). Use the generator's 7 points for both definition and collision. | no |
| 7 | **Class B bulkhead vs Class B interior — blockout read (G3 / pack note).** Pack asks to keep the mockup’s visual distinction between riveted bulkhead bands and ordinary solid faces while both behave as Class B. For P1 blockout materials, should bulkheads use a darker/heavier steel (or thicker extrusion) versus interiors, and may Class A partitions use a third readable treatment — or is a single hull-steel material with accent trim sufficient until P3 damage states? | **Three readable treatments, all in-palette:** (a) **Class B interior** — standard hull-steel `#1a232e`–`#2a3644`, single thickness; (b) **Class B bulkhead** — `#2c3947` with `#5c7186` trim (rivet-band motif from the mockup), **double wall thickness** — bulkheads must read as law at a glance; (c) **Class A partition** — hull-steel with `#39434e` cap/trim + a thin section-accent edge strip (reads as "partition," seeds the P3 breachable identity). **Class C hull** — hull-steel gradient range with armor-trim `#5c7186` inset motif. No new colors; all values from mockup v2.2 / VD §3. | no |
| 8 | **Wall class on non-blast door edges (§7 r7 / M1).** Every door edge must carry a wall class. Blast doors are Class B (R1 / pack). What class do the other 15 door edges carry (spine↔room, Engineering↔engines, CIC↔adjacent, connector↔CIC)? Spec provisionally types them `A` (passable partition openings). Confirm or assign the binding class per edge kind. | **Rejected provisional. All 15 non-blast door edges = Class B (interior).** They pierce ordinary solid walls (Q1), and Class A carries breachability/damage semantics (M2) that must never attach to a doorway. Blast doors = Class B (bulkhead presentation). Result: every non-A edge in the graph is immune — the §7 r7 firewall stays coherent, and P2/P3 behavior layers read door edges as indestructible-but-sealable (blast) or indestructible (rest) without re-typing. | no |

*Status: ☑ all questions resolved — spec may finalize. Doc changes applied: design doc v1.10 → **v1.11** (§6.1 Class B presentation split); visual direction v1.1 → **v1.2** (§3 extended accent table).*
