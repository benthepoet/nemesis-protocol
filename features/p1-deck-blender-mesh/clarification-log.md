# Clarification Log — p1-deck-blender-mesh

`Working versions: charter v1.13 · design doc v1.19 · visual direction v1.10 · roadmap v1.9 · assets/AGENTS.md (current) · p1-ship-blender-viz exploration pack v0 · p1-deck-blender-mesh design pack v1 · integration @ e38ab8c`

*Kimi answers (2026-07-31) are binding design rulings (charter §3.2a). All twelve resolve against **design pack v1** — no design-doc or VD changes required (presentation-only feature).*

| ID | From | To | Question | Answer | Status |
|----|------|-----|----------|--------|--------|
| Q1 | Grok | Kimi | **Design pack v1 missing.** Please publish `features/p1-deck-blender-mesh/design-pack.md` **DESIGN PACK v1** with numbered goal set (G#) and mechanics callout (M#) per charter §3.1 / template. Without G#/M#, SPEC cannot FINAL (traceability gate). Confirm presentation-only + collision/deck-graph freeze as goals. | **Published.** `DESIGN PACK v1 | feature p1-deck-blender-mesh | design doc v1.19 | goals: G1..G7 | mechanics: M1..M5`. Presentation-only + collision/deck-graph freeze are explicit: **G6** (zero functional drift, collision from `deck03.json` only) and **R-BM1**. [doc change: no] | **resolved** |
| Q2 | Grok | Kimi | **Default vs flag.** Director kickoff names `?deckVisual=blender`. Confirm: (a) default boot remains procedural `createDeckScene` unless URL/`VITE_` override; (b) accepted values (`blender` / `procedural` / omit); (c) is blender intended to become default this feature, or stay opt-in through Gate 2? Mirror pattern of `?heroMaterial=`. | **Ruling R-BM4 (G4): GLB is the DEFAULT on merge** — (a) is overruled. Rationale: the GLB is the intended ship look; Gates 1–2 must review the default experience, and Stage 4 verifies it before merge. Accepted values: `?deckVisual=blender` / `?deckVisual=procedural` / omit (= blender), mirroring `?heroMaterial=`. Procedural stays as opt-out/fallback; its retirement is a Director decision deferred to the #11 kickoff (earliest). [doc change: no] | **resolved** |
| Q3 | Grok | Kimi | **Prereq GLB ownership & path.** Who delivers the shipped asset and where? Proposal for TL outline: Kimi exports `assets/models/p1_nemesis_deck03.glb` (CC BY 4.0, `assets/AGENTS.md` naming) from `assets/blender/p1_nemesis_deck03_viz.blend` (+ regenerate via `tools/blender/build_p1_nemesis_deck03_viz.py` when needed). Confirm path, filename, and whether Composer may only *load* (not author) the GLB. | **Confirmed as proposed.** Kimi owns and delivers `assets/models/p1_nemesis_deck03.glb` (single file, R-BM5), CC BY 4.0, exported from the committed `.blend` via the amended generator (Q9). Composer **loads only** — never authors, edits, or regenerates the GLB (charter §1: Composer does not modify asset files). [doc change: no] | **resolved** |
| Q4 | Grok | Kimi | **Supersede surface when blender path is on.** Kickoff: "supersedes procedural `createDeckScene` hull/room read." Confirm exact replace set: rooms (floors/ceilings), walls A/B/C, hull envelope + interstitial, dressing/props, trim/signage? What stays engine-owned: `createDeckLighting`, aperture reserved-face tags, actor/hero meshes, combat VFX, HUD? Must kit-bash `buildDeckDressing` be **skipped** entirely on blender path (else double dressing)? | **Replace set (GLB owns):** room floors/ceilings, walls A/B/C, hull envelope + interstitial slab + `hull-interstitial:ceiling` face, static dressing/props, static trim strips. **`buildDeckDressing` is skipped entirely on the blender path** — no double dressing. **Stays engine-owned:** `createDeckLighting` (GLB carries no lights, R-BM7), **signage atlas planes** (`signage:<roomId>` — GLB `SIGNAGE` collection is export-excluded, R-BM3), aperture *tag contract* (the GLB ships the tagged faces, M3/G3), actor/hero meshes, combat VFX/telegraphs, HUD, audio, collision/sim everything. [doc change: no] | **resolved** |
| Q5 | Grok | Kimi | **Ceiling cutaway contract.** Engine `updateCeilingCutaway` requires `roomGroups` with meshes named `ceiling:<roomId>` (all 18 deck03 ids) plus optional interstitial ceiling Object3D. Viz script uses `ceil_<rid>` / `ceil_bridge` / `deckhead` in collection `CEILINGS`. Confirm export/import rename map to `ceiling:<id>` (bridge → `ceiling:bridge`), which object is interstitial (`deckhead`?), and that fade opacity bands (active=0 / adj∪frustum=`CEILING_FADE_ALPHA` / else=1) still apply unchanged (M8 from deck-polish). | **Confirmed (M2/G2).** Rename happens **at export in Blender** (pack pipeline amendment 1), not at import: all 18 rooms get `room:<id>` grouping + `ceiling:<id>` meshes (bridge → `ceiling:bridge`); `deckhead` renames to **`hull-interstitial:ceiling`** and ships in the GLB so the existing `boot.ts` binding finds it. Fade bands are engine constants and apply **unchanged** — the GLB only supplies geometry; `updateCeilingCutaway` is not touched (R-BM2). [doc change: no] | **resolved** |
| Q6 | Grok | Kimi | **Axis / origin / scale lock.** Viz pack locks `SCALE_PX=120→25m`, `ROOM_HEIGHT_M=3.0`, Blender Z-up from `svgToWorld`. Confirm engine import must apply a single rigid transform (Z-up→Y-up) with **zero** additional offset/scale, and that room footprints align to `deck03.json` world coords within a stated epsilon. | **Confirmed (R-BM6).** Exporter writes engine world coords directly (meters, y-up glTF standard, x→X / deck-y→Z per `svgToWorld`, origin = viz `CX,CY` lock); import applies **no** offset/scale beyond the standard glTF y-up convention. Audit epsilon: **≤ 0.05 m** AABB drift vs `deck03.json` footprints, audit-only (never collision) — your proposal accepted; pack amended. Target is exact, since both sides run the same conversion math. [doc change: no] | **resolved** |
| Q7 | Grok | Kimi | **Materials & lighting authority.** On blender path: keep authored glTF PBR as-is, remap to `DeckMaterialSet`, or hybrid? Keep runtime `createDeckLighting` + AL0/AL1 presentation, or rely on GLB emissives only? Any requirement to strip optional maps (`stripOptionalMaterialMaps`) for TU budget like heroes? | **Hybrid (M5/R-BM7).** GLB ships standard PBR (Principled → glTF); engine may retune values on load — ceiling transparency setup for M2, emissive intensities for trim/gels, and remapping to the delivered `p1_*` texture sets where the GLB is untextured — but palette and VD §3 color meanings are locked. **Lighting authority stays 100 % engine:** `createDeckLighting` + AL0/AL1 rows unchanged; GLB emissives are accents only, never a light source substitute. `stripOptionalMaterialMaps` is permitted at your discretion for TU budget, provided the VD §5 read holds at Gate 1 (B2 luminance audit still binds). [doc change: no] | **resolved** |
| Q8 | Grok | Kimi | **Load failure / fallback.** If GLB missing or `loadGltf` rejects: hard-fail boot, or silent fall back to procedural `createDeckScene` with console error? Gate 1 / CI expectation? | **Fall back, never hard-fail (G4/R-BM4):** procedural `createDeckScene` boots with a console error naming the missing asset. Gate 1 evidence: B7 — forced-missing-asset boot shows fallback + warning. CI expectation: default (blender) path must load in the CI smoke test; the fallback path is exercised by the forced-missing test. [doc change: no] | **resolved** |
| Q9 | Grok | Kimi | **Export pipeline in-repo.** Is a documented Blender CLI/export step (or extension of `build_p1_nemesis_deck03_viz.py`) a prereq-asset deliverable from Kimi this feature, or is a checked-in GLB alone sufficient? | **Reproducible export is required (R-BM8).** Kimi amends `tools/blender/build_p1_nemesis_deck03_viz.py` with an `export_glb()` step (pack §pipeline amendments 1–6) and commits all three: script, `.blend` workfile, GLB. GLB is regenerated from the script, never hand-edited (assets/AGENTS.md rule 1 spirit). A checked-in GLB alone is **not** sufficient. [doc change: no] | **resolved** |
| Q10 | Grok | Kimi | **Acceptance / Gate 1 visuals.** Which screenshots / reads prove blender path (reuse ship-viz R1–R5 framing vs in-engine capture)? Any luminance / silhouette void-pixel bar from deck-polish G1 that still binds? | **Pack acceptance plan B1–B8.** In-engine captures (not Blender renders) at the 60° gameplay camera: B1 reuses viz R1 framing side-by-side vs procedural; B5 reuses R2/R4 framings in-engine; B8 re-runs the Director's `gate2-screen0..3` failing views. **Yes — the deck-polish G1/R-DP18 void-pixel luminance bar still binds** on the blender path (B2: full-deck sweep at AL0 *and* AL1 + pixel audit; VD §7 rule 9). [doc change: no] | **resolved** |
| Q11 | Grok | Kimi | **Determinism / hash scope.** Confirm zero sim drift: `hashSimState` + `hashDeckGraph` + `buildCollisionWorld` bit-identical with blender path enabled vs procedural. Visual-scene hash (if any) is out of sim hash — OK to assert only that presentation modules do not mutate sim/deck/collision? | **Confirmed (G6/R-BM1).** All three hashes bit-identical across paths. Visual-scene contents are out of sim-hash scope by definition — the assertion you propose is the correct one: presentation modules (GLB loader, material retune, cutaway bindings) must not mutate sim, deck graph, or collision world. Add it as a spec verification item. [doc change: no] | **resolved** |
| Q12 | Grok | Kimi | **Out of scope confirm.** Still deferred: starfield / hull-exterior gameplay (#11), wall damage states (P3), actor figures in deck GLB, multi-deck, alarm lighting beyond existing AL0/AL1 engine lights, any collision or door/graph edits. | **Confirmed**, plus two additions: GLB `SIGNAGE` text is excluded (engine atlas stays the single signage source, R-BM3), and procedural-path retirement is deferred to the #11 kickoff (R-BM4). #11 (`p1-hull-exterior-view`) remains under Director hold — this feature ships **no** starfield and **no** aperture transparency; its exterior bar is the solid-hull read only (pack §non-goals, §relationship-to-#11). [doc change: no] | **resolved** |
